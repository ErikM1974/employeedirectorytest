feat(artwork): improve sorting to prioritize due dates

- Sort artwork records by status first
- Within each status, sort by due date (earliest first)
- Records without due dates appear at end of each status group
- Use ID_Design DESC as final sort to show newest records first
- Filter to show records with ID_Design > 52000
